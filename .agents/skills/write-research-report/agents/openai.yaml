interface:
  display_name: "Write Research Report"
  short_description: "Turn research evidence into reusable Markdown"
  default_prompt: "Use $write-research-report to write a reusable Why-then-How Markdown report, linking modeling, training, and benchmarks to the problem each addresses."
